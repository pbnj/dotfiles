#!/bin/bash

MSG="$1"
TITLE="$2"

if [ -z $MSG ] || [ -z $TITLE ]; then
  echo "Usage: notify <message> <title>"
  exit 1
fi

/usr/bin/osascript -e "display notification \"$MSG\" with title \"$TITLE\""
