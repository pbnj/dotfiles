# Generic
alias ..="cd .."
alias ...="cd ../.."

# For safety
alias rm='rm -i'
alias cp='cp -i'
alias mv='mv -i'

# Detect which `ls` flavor is in use
alias sudo='sudo'
alias ls="ls -alFh"
alias grep="grep --color=auto"
